# Docker build: same as requirements.txt but without pyrfc (pyrfc is not installed in the image)
mcp>=1.0.0
click>=8.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
cryptography>=41.0.0
psutil>=5.9.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
